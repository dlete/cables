<%= form_for(@circuit) do |f| %>
  <% if @circuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@circuit.errors.count, "error") %> prohibited this circuit from being saved:</h2>

      <ul>
      <% @circuit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reference %><br />
    <%= f.text_field :reference %>
  </div>
  <div class="field">
    <%= f.label :provider %><br />
    <%= f.collection_select :provider_id, @providers,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :end_a %><br />
    <%= f.collection_select :end_a, @endpoints,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :end_b %><br />
    <%= f.collection_select :end_b, @endpoints,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
