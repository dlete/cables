<%= form_for(@circuit) do |f| %>
  <% if @circuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@circuit.errors.count, "error") %> prohibited this circuit from being saved:</h2>

      <ul>
      <% @circuit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reference %><br />
    <%= f.text_field :reference %>
  </div>
  <div class="field">
    <%= f.label :provider_id %><br />
    <%= f.collection_select :provider_id, @providers,
                            :id, :organization_id,
                            :include_blank => true %>

  <div class="field">
    <%= f.label :end_a_endpoint_id %><br />
    <%= f.collection_select :end_a_endpoint_id, @endpoints,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :end_a_medium_id %><br />
    <%= f.collection_select :end_a_medium_id, @media,
                            :id, :name,
                            :include_blank => true %>
  </div>

  <div class="field">
    <%= f.label :end_b_endpoint_id %><br />
    <%= f.collection_select :end_b_endpoint_id, @endpoints,
                            :id, :name,
                            :include_blank => true %>
  </div>
  <div class="field">
    <%= f.label :end_b_medium_id %><br />
    <%= f.collection_select :end_b_medium_id, @media,
                            :id, :name,
                            :include_blank => true %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
